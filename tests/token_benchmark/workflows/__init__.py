"""Controlled workflow benchmark: fixtures, transport, evaluation, and
repair-aware trial orchestration for the `workflow` suite.

Separate from the academic MMLU/HumanEval/HellaSwag/TruthfulQA suite in
`tests/token_benchmark/{benchmarks,scorer}.py`; see `tests/token_benchmark
/workflows/fixtures/manifest.json` for the frozen fixture inputs.
"""
