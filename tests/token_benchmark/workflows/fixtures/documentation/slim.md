# Documentation Constraints

Use only supplied repository facts. Produce a concise README with requirements and commands, plus a machine-readable
command map. Do not invent installation, license, support, or maturity claims. Return the requested JSON shape.
