# Implementation Constraints

Implement only the requested pure function. Satisfy every public criterion, preserve the signature, and return exactly
one `solution.py` artifact in the requested JSON shape. Do not access files, the network, environment, or subprocesses.
