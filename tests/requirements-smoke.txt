# Opt-in runtime deps for the smoke-test orchestrator (installed only when smoke is enabled).
# Browser install is Chromium-only and idempotent (see plan research R1).
#
# Pins verified importable on the repo's CI Python (3.14): playwright 1.49.x hard-pins
# greenlet==3.1.1, which has no cp314 wheel and fails to build from source on 3.14.
# 1.62.0 pulls greenlet>=3.5 (cp314 wheels) and installs cleanly.
playwright==1.62.0
PyYAML==6.0.3
