# CI dependency pins (specs/003 FR-014) — keep in sync with .pre-commit-config.yaml
# where the same tool appears (yamllint matches pre-commit's v1.38.0).
yamllint==1.38.0
pytest==8.3.4
pytest-asyncio==0.25.2
pyyaml==6.0.3
rich==13.9.4
asyncio-throttle==1.0.2
