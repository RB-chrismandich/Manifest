---
description: "DEPRECATED — superseded by the smoke-orchestrator skill (UI steps with mode agent). Kept one release for migration. Manages legacy browser-use YAML prompts in tests/browser/; migrate via python3 -m smoke_orchestrator.migrate tests/browser --app <app>."
globs: ".claude/skills/browser-test/**"
alwaysApply: false
---

# browser-test

DEPRECATED — superseded by the smoke-orchestrator skill (UI steps with mode agent). Kept one release for migration. Manages legacy browser-use YAML prompts in tests/browser/; migrate via python3 -m smoke_orchestrator.migrate tests/browser --app <app>.

<!-- Auto-generated from .claude/skills/browser-test/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/browser-test/SKILL.md` for the full skill definition.

