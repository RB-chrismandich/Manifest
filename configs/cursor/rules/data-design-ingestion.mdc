---
description: "Use when designing a local table that caches records fetched from an external feed — choose append-only+dedup vs full-replace based on whether the upstream data is immutable history or re-published-in-full."
globs: ".claude/skills/data-design-ingestion/**"
alwaysApply: false
---

# data-design-ingestion

Use when designing a local table that caches records fetched from an external feed — choose append-only+dedup vs full-replace based on whether the upstream data is immutable history or re-published-in-full.

<!-- Auto-generated from .claude/skills/data-design-ingestion/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/data-design-ingestion/SKILL.md` for the full skill definition.

