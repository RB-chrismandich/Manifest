---
description: "Full Manifest regression (CI mirror: shellcheck, yamllint, bats, pytest) plus a deployed-env smoke pass — the post-PR gate. Use when a PR opens or merges: \"regression test the PR\", \"did the merge break anything\", \"verify main is still green\". Whole-repo verdict; prefer over verify or env-check."
globs: ".claude/skills/pr-smoke/**"
alwaysApply: false
---

# pr-smoke

Full Manifest regression (CI mirror: shellcheck, yamllint, bats, pytest) plus a deployed-env smoke pass — the post-PR gate. Use when a PR opens or merges: "regression test the PR", "did the merge break anything", "verify main is still green". Whole-repo verdict; prefer over verify or env-check.

<!-- Auto-generated from .claude/skills/pr-smoke/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/pr-smoke/SKILL.md` for the full skill definition.

