---
description: "Use before deploying any app that parses its own config file (Glance, nginx, Traefik, Prometheus, Terraform, etc.) — validate with the application's OWN parser, not a generic linter, because generic linting passes configs the app rejects."
globs: ".claude/skills/app-native-config-validation/**"
alwaysApply: false
---

# app-native-config-validation

Use before deploying any app that parses its own config file (Glance, nginx, Traefik, Prometheus, Terraform, etc.) — validate with the application's OWN parser, not a generic linter, because generic linting passes configs the app rejects.

<!-- Auto-generated from .claude/skills/app-native-config-validation/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/app-native-config-validation/SKILL.md` for the full skill definition.

