---
description: "Design tests that replace real home, XDG state, network, installed harnesses, and repository globals with explicit fixtures so broken code cannot pass on ambient state."
globs: ".claude/skills/test-isolate-ambient/**"
alwaysApply: false
---

# test-isolate-ambient

Design tests that replace real home, XDG state, network, installed harnesses, and repository globals with explicit fixtures so broken code cannot pass on ambient state.

<!-- Auto-generated from .claude/skills/test-isolate-ambient/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/test-isolate-ambient/SKILL.md` for the full skill definition.

