---
description: "Use before verifying Manifest deploy, hook, or installer behaviour — a pass against your real ~/.claude, settings.json, or checked-out repo proves nothing; the ambient state IS the fixture. Provides isolation handles (TARGET_DIR, ISSUE_HOOKS_SETTINGS, throwaway clone) and the rule for making the varying thing vary."
globs: ".claude/skills/test-isolate-ambient/**"
alwaysApply: false
---

# test-isolate-ambient

Use before verifying Manifest deploy, hook, or installer behaviour — a pass against your real ~/.claude, settings.json, or checked-out repo proves nothing; the ambient state IS the fixture. Provides isolation handles (TARGET_DIR, ISSUE_HOOKS_SETTINGS, throwaway clone) and the rule for making the varying thing vary.

<!-- Auto-generated from .claude/skills/test-isolate-ambient/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/test-isolate-ambient/SKILL.md` for the full skill definition.

