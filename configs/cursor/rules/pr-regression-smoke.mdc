---
description: "Full Manifest regression (CI mirror: shellcheck, yamllint, markdownlint, bats + pytest) plus a deployed-env smoke pass, as a post-PR gate. Use right after a PR opens or merges — \"regression test the PR\", \"did the merge break anything\", \"verify main is still green\". Whole-repo verdict; prefer over verify or health-check."
globs: ".claude/skills/pr-regression-smoke/**"
alwaysApply: false
---

# pr-regression-smoke

Full Manifest regression (CI mirror: shellcheck, yamllint, markdownlint, bats + pytest) plus a deployed-env smoke pass, as a post-PR gate. Use right after a PR opens or merges — "regression test the PR", "did the merge break anything", "verify main is still green". Whole-repo verdict; prefer over verify or health-check.

<!-- Auto-generated from .claude/skills/pr-regression-smoke/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/pr-regression-smoke/SKILL.md` for the full skill definition.

