---
description: "Use before finalizing spec/plan/tasks — cross-reference for internal consistency via the parallel-agent panel (excluding the author), deduped findings. Analysis-only, never edits. Speckit/superpowers; auto-discovers or explicit paths."
globs: ".claude/skills/spec-review/**"
alwaysApply: false
---

# spec-review

Use before finalizing spec/plan/tasks — cross-reference for internal consistency via the parallel-agent panel (excluding the author), deduped findings. Analysis-only, never edits. Speckit/superpowers; auto-discovers or explicit paths.

<!-- Auto-generated from .claude/skills/spec-review/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/spec-review/SKILL.md` for the full skill definition.

