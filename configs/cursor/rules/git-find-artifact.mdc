---
description: "Use when a referenced file (spec, plan, doc, config) does not exist at the given path on the current branch — search worktrees, all branches, and git history before assuming it is missing or asking the user."
globs: ".claude/skills/git-find-artifact/**"
alwaysApply: false
---

# git-find-artifact

Use when a referenced file (spec, plan, doc, config) does not exist at the given path on the current branch — search worktrees, all branches, and git history before assuming it is missing or asking the user.

<!-- Auto-generated from .claude/skills/git-find-artifact/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/git-find-artifact/SKILL.md` for the full skill definition.

