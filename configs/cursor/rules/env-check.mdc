---
description: "Diagnoses Manifest environment health — CLI tools, auth, config syntax, MCP, symlinks — when setup seems broken or post-bootstrap."
globs: ".claude/skills/env-check/**"
alwaysApply: false
---

# env-check

Diagnoses Manifest environment health — CLI tools, auth, config syntax, MCP, symlinks — when setup seems broken or post-bootstrap.

<!-- Auto-generated from .claude/skills/env-check/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/env-check/SKILL.md` for the full skill definition.

