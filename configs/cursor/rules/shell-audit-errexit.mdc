---
description: "Use when a bash script under `set -euo pipefail` aborts in production but passes tests — audit helpers and sourced libs for non-`$()` control-flow triggers (trailing `&&`, stdin-drain, SIGPIPE, `((i++))`). Complements shell-audit-pipefail."
globs: ".claude/skills/shell-audit-errexit/**"
alwaysApply: false
---

# shell-audit-errexit

Use when a bash script under `set -euo pipefail` aborts in production but passes tests — audit helpers and sourced libs for non-`$()` control-flow triggers (trailing `&&`, stdin-drain, SIGPIPE, `((i++))`). Complements shell-audit-pipefail.

<!-- Auto-generated from .claude/skills/shell-audit-errexit/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/shell-audit-errexit/SKILL.md` for the full skill definition.

