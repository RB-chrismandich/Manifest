---
description: "Use when a containerized app crash-loops or errors on \"variable not found\"/empty-value despite the orchestrator validating fine — the app does its OWN ${VAR} substitution on mounted config, separate from compose/k8s interpolation."
globs: ".claude/skills/debug-layered-config-substitution/**"
alwaysApply: false
---

# debug-layered-config-substitution

Use when a containerized app crash-loops or errors on "variable not found"/empty-value despite the orchestrator validating fine — the app does its OWN ${VAR} substitution on mounted config, separate from compose/k8s interpolation.

<!-- Auto-generated from .claude/skills/debug-layered-config-substitution/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/debug-layered-config-substitution/SKILL.md` for the full skill definition.

