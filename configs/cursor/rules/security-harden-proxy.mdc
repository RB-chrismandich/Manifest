---
description: "Use when building a service or sidecar that calls an authenticated upstream API (Bearer token, API key) and relays results, to prevent the credential or full URL from leaking into logs or client responses"
globs: ".claude/skills/security-harden-proxy/**"
alwaysApply: false
---

# security-harden-proxy

Use when building a service or sidecar that calls an authenticated upstream API (Bearer token, API key) and relays results, to prevent the credential or full URL from leaking into logs or client responses

<!-- Auto-generated from .claude/skills/security-harden-proxy/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/security-harden-proxy/SKILL.md` for the full skill definition.

