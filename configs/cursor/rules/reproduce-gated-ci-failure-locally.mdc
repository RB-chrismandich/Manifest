---
description: "Use when a CI job fails but the run's logs are gated ("still in progress") or hard to read — pinpoint the failing step via the jobs API and reproduce that step's commands locally from the workflow file."
globs: ".claude/skills/reproduce-gated-ci-failure-locally/**"
alwaysApply: false
---

# reproduce-gated-ci-failure-locally

Use when a CI job fails but the run's logs are gated ("still in progress") or hard to read — pinpoint the failing step via the jobs API and reproduce that step's commands locally from the workflow file.

<!-- Auto-generated from .claude/skills/reproduce-gated-ci-failure-locally/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/reproduce-gated-ci-failure-locally/SKILL.md` for the full skill definition.

