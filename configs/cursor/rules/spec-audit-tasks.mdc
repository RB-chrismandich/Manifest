---
description: "After an implement step, audit that every task was genuinely completed — catch skipped tasks, stubbed work, missing tests, unimplemented spec requirements. Works with speckit (tasks.md) and superpowers layouts; auto-discovers or takes paths. Runs as the speckit after_implement hook."
globs: ".claude/skills/spec-audit-tasks/**"
alwaysApply: false
---

# spec-audit-tasks

After an implement step, audit that every task was genuinely completed — catch skipped tasks, stubbed work, missing tests, unimplemented spec requirements. Works with speckit (tasks.md) and superpowers layouts; auto-discovers or takes paths. Runs as the speckit after_implement hook.

<!-- Auto-generated from .claude/skills/spec-audit-tasks/SKILL.md -->
<!-- Regenerate with: .claude/scripts/generate_cursor_rules.sh -->

Refer to `.cursor/skills/spec-audit-tasks/SKILL.md` for the full skill definition.

