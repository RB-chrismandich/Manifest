# T045/FR-029 — the apm pin that has actually been verified.
#
# This file is the upgrade gate's subject. tests/bats/apm_upgrade_gate.bats
# asserts it matches APM_PINNED_VERSION / APM_WHEEL_SHA256 in
# bootstrap/lib/install.sh, so BUMPING THE PIN WITHOUT UPDATING THIS FILE FAILS
# THE SUITE. That is the whole mechanism: a version bump cannot land quietly, it
# has to be accompanied by someone recording that they re-verified it.
#
# To upgrade, in this order:
#   1. Re-run the deployment matrix against the new version in an isolated HOME
#      (specs/522-apm-deploy-migration/decision-record.md documents the cells).
#   2. Confirm idempotence: install twice, assert byte-identical.
#   3. Confirm equivalence: the new version's output matches the old one's for
#      an unchanged source tree.
#   4. Record the digest below, from the PyPI JSON API, verified before install.
#
# Skipping 1-3 and editing only this file defeats the gate. The gate makes the
# omission visible; it cannot make it impossible.
version = 0.26.0
sha256 = 76290c42a9f9412e3ae8ea98d20acbdfbf4bee6dc32e951f616feab9bc82e7ad
verified_on = 2026-07-27
verified_by = T001 spike + live PyPI re-check during T054
