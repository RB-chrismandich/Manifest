schema_version: 1
bundle:
  name: stitch-design
  version: 0.3.0
  description: Design generation, review, extraction, and implementation workflows.
  category: design
components:
  skills:
    root: skills
    include: ["*/SKILL.md"]
  agents:
    - {id: design-lens-reviewer, path: agents/design-lens-reviewer.md}
    - {id: skeptic-verifier, path: agents/skeptic-verifier.md}
    - id: ui-builder
      path: agents/ui-builder.md
      compatibility:
        claude: {mode: unsupported, reason: OMP-only constrained tool enforcement}
        codex: {mode: unsupported, reason: OMP-only constrained tool enforcement}
        gemini: {mode: unsupported, reason: OMP-only constrained tool enforcement}
        cursor: {mode: unsupported, reason: OMP-only constrained tool enforcement}
        antigravity: {mode: unsupported, reason: OMP-only constrained tool enforcement}
        devin: {mode: unsupported, reason: OMP-only constrained tool enforcement}
    - id: ui-reviewer
      path: agents/ui-reviewer.md
      compatibility:
        claude: {mode: unsupported, reason: OMP-only read-only tool enforcement}
        codex: {mode: unsupported, reason: OMP-only read-only tool enforcement}
        gemini: {mode: unsupported, reason: OMP-only read-only tool enforcement}
        cursor: {mode: unsupported, reason: OMP-only read-only tool enforcement}
        antigravity: {mode: unsupported, reason: OMP-only read-only tool enforcement}
        devin: {mode: unsupported, reason: OMP-only read-only tool enforcement}
  hooks: []
  runtime:
    - {id: stitch-dist, path: runtime/dist}
    - {id: stitch-build, path: runtime/node/build.mjs}
    - {id: stitch-build-lock, path: runtime/node/package-lock.json}
    - {id: stitch-build-package, path: runtime/node/package.json}
    - {id: static-html-sources, path: skills/extract-static-html/scripts}
    - {id: model-qualification, path: runtime/model-qualification}
    - {id: react-validator, path: skills/react-components/scripts}
    - {id: react-native-validator, path: skills/react-native/scripts}
    - id: stitch-omp-package
      path: package.json
      compatibility:
        claude: {mode: unsupported, reason: OMP package manifest}
        codex: {mode: unsupported, reason: OMP package manifest}
        gemini: {mode: unsupported, reason: OMP package manifest}
        cursor: {mode: unsupported, reason: OMP package manifest}
        antigravity: {mode: unsupported, reason: OMP package manifest}
        devin: {mode: unsupported, reason: OMP package manifest}
    - id: stitch-omp-mcp-definition
      path: .mcp.json
      compatibility:
        claude: {mode: unsupported, reason: OMP MCP definition}
        codex: {mode: unsupported, reason: OMP MCP definition}
        gemini: {mode: unsupported, reason: OMP MCP definition}
        cursor: {mode: unsupported, reason: OMP MCP definition}
        antigravity: {mode: unsupported, reason: OMP MCP definition}
        devin: {mode: unsupported, reason: OMP MCP definition}
    - id: stitch-omp-extension
      path: extensions/ui-delivery-policy.ts
      compatibility:
        claude: {mode: unsupported, reason: OMP extension}
        codex: {mode: unsupported, reason: OMP extension}
        gemini: {mode: unsupported, reason: OMP extension}
        cursor: {mode: unsupported, reason: OMP extension}
        antigravity: {mode: unsupported, reason: OMP extension}
        devin: {mode: unsupported, reason: OMP extension}
  guidance: []
capabilities:
  mcp:
    required: []
    default: []
    optional: [stitch]
  executables:
    required: [bash, git, python3]
    default: [node]
    optional: [chromium, curl]
compatibility:
  claude: {mode: native}
  codex: {mode: native}
  gemini: {mode: generated}
  cursor: {mode: generated}
  antigravity: {mode: imported}
  devin: {mode: native}
provenance:
  repository: https://github.com/RB-chrismandich/Manifest
  license: MIT
  license_file: LICENSE
  generated_by: tools/generate_plugin_views.py
